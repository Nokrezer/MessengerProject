import {Text, StyleSheet} from "react-native";

export default function RegForm(){
    return (
        <Text style={styles.text}>reg form</Text>
    );
}

const styles = {
    text:{
        color:"white"
    }
};